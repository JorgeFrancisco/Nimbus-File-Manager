HELP.md

# Project
/target/
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/
/workspace/
.*.txt
.*.zip
.*.rar

### Docker ###
.env
/library/

### Eclipse / Spring Tool Suite ###
.apt_generated
.classpath
.factorypath
.project
.settings/*
!.settings/org.eclipse.jdt.core.prefs
!.settings/org.eclipse.core.resources.prefs
!.settings/org.eclipse.jdt.ui.prefs
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea/
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
/build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

# AI tooling (local configuration)
.codex/
.agents/

# Third-party binaries the application fetches for itself. The whole folder is downloaded and
# recreatable: too large for git, and the licences that come with the binaries are not kept here
# because this repository distributes none of them. This covers every subfolder, deliberately
# without naming them - a list here would become a lie with the first tool added. See the README.
/tools/
# FUSE mount artefacts (deleted files still held open)
.fuse_hidden*